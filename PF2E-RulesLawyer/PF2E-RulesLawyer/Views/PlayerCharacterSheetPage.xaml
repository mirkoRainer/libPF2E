<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PF2E_RulesLawyer.Views.PlayerCharacterSheetPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <StackLayout Orientation="Horizontal">
            <Label Text="{Binding CharacterName}" d:Text="Croft" FontSize="Medium" HorizontalOptions="Start" />
            <Label Text="{Binding Ancestry}" d:Text="Dwarf" FontSize="Medium" HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding PcClass}" d:Text="Rogue 1" FontSize="Medium" HorizontalOptions="End" />
        </StackLayout>
        <StackLayout Orientation="Horizontal">
            <Label Text="{Binding PlayerName}" d:Text="Gary" FontSize="Medium" HorizontalOptions="Start" />
            <Label Text="{Binding Heritage}" d:Text="Anvil Dwarf" FontSize="Medium" HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding SubClass}" d:Text="Ruffian" FontSize="Medium" HorizontalOptions="End" />
        </StackLayout>
    </StackLayout>
</ContentPage>